eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
server:
  port: 8091
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-a:
      path: /produce1/**
      serviceId: produce1
    api-b:
      path: /client/**
      serviceId: eureka-client
